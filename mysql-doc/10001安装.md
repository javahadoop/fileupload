#mysql安装
# yum -y install mysql*        --mysql安装 
# service mysqld start         --mysql启动
# chkconfig mysqld on          --mysql添加linux开机 启动
# mysqladmin -u root password root    -- 设置mysql帐号密码
# mysql -u root -p            --登录mysql


#mysql 基本操作
